.zig-cache/
zig-out/
zig-cache/
node_modules/
*.log
.DS_Store
# test/harness.test.mjs creates per-run tmp dirs under the repo root so
# Node's package self-resolve picks up "capnwasm" from package.json.
.tmp-harness-test-*/

# Per-assistant project files. Kept local; never commit.
.claude/
CLAUDE.md
CLAUDE.local.md
.claude.json
.mcp.json
.opencode/
.cursor/
.cursorrules
.aider*
.continue/
.windsurf/
.codeium/
.github/copilot-instructions.md
AGENTS.local.md
HANDOFF.md

# Wrangler local state
.wrangler/
.capnwasm-zig

# Large external schemas pulled in for round-trip tests. The
# openapi_cloudflare_roundtrip test skips when these are absent and
# prints the curl command to fetch them.
test/_fixtures/cloudflare-openapi.json
